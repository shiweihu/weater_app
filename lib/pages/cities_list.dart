import 'package:flutter/material.dart';

class CitiesListComponent extends StatelessWidget{
  const CitiesListComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}